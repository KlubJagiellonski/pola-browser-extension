{
  "name": "pola-browser-extension",
  "version": "1.0.0",
  "description": "Pola - sprawdź polskość produktu po kodzie EAN",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:integration": "xvfb-run npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
